language: r
cache: packages
r:
  - oldrel
  - release
  - devel
addons:
  apt:
    packages:
    - libv8-dev
    - libhdf5-serial-dev
    - libgdal1-dev
    - libgdal-dev
r_github_packages:
  - nealrichardson/httptest
after_success:
  - Rscript -e 'covr::codecov()'
