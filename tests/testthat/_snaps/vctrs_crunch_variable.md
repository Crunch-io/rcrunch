# typed_crunch_variable validation

    Unexpected variable structure a string & no axes defined

# Validation works during setting variable attributes

    `value` must be <character> (got NULL)

---

    `value` must be length 1 (got length 2)

---

    `value` must be <character> (got a number)

---

    `value` must be <character> (got a <formula> object)

---

    `value` must be <character> (got a function)

